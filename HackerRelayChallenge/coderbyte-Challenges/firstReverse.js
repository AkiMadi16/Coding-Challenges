function FirstReverse(str) { 
  
  return str.split('').reverse().join(''); 

}
   
// keep this function call here 
console.log(FirstReverse("Hello World and Coders")); //=> sredoC dna dlroW olleH

// The optimal runtime for this challenge is O(n)

// Score - This user scored higher than 24.6% of users who solved this challenge.
// Time - This user solved this challenge in 2 minutes. The majority of users solved it in about 0-9 minutes.

// runtime Analysis

// constant 0(1)
// Logarithmic O(log n)
//  Linear O(n)
// Loglinear O(n log n)
// Quadratic O(n^2)
// Polynomial O(n^c)
// Exponential O(c^n)
// Factorial O(n!)

// Given runtime is the optimal runtime in Big-O notation